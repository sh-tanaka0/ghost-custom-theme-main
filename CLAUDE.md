# CLAUDE.md

このファイルはClaude Code (claude.ai/code) がこのリポジトリでコードを扱う際のガイダンスを提供します。

## テーマ概要

「icily-theme」は、日本語コンテンツとマルチメディア表現を特徴とするクリエイティブコンテンツサイト向けのGhost CMSテーマです。このテーマは、洗練された冬をテーマとした美学で、ストーリーテリング、ビジュアルアート、文学作品に焦点を当てています。

## 主要アーキテクチャ

### テンプレート構造
- `default.hbs` - ヘッダー、ナビゲーション、フッターを含むメインレイアウトテンプレート
- `home.hbs` - 特集コンテンツ、エッセイ、連載作品、レビュー、アートギャラリーのセクションを含むホームページ
- `post.hbs` - 読書設定、サポートセクション、関連記事を含む個別記事テンプレート
- `page.hbs` - 静的ページテンプレート
- カスタムテンプレート:
  - `custom-novel-series.hbs` - 小説シリーズハブページ用
  - `custom-series-index.hbs` - シリーズインデックスページ用
  - `custom-dot-art.hbs` - ピクセルアートギャラリー用
  - `writings.hbs` - 創作物アーカイブ
  - `gallery.hbs` - アートギャラリー
  - `archives.hbs` - 全記事アーカイブ

### コンテンツ組織化
テーマは洗練されたタグベースのコンテンツシステムを使用します：
- `rambles` - 思考・エッセイ
- `review` - 批評・社会評論
- `dot-art` - ピクセルアートコンテンツ
- `serial-hub` - 小説シリーズハブページ（投稿ではなくページとして）
- `complete` - 完結作品
- `short` - 短編小説
- `word-of-the-day` - 今日のひとこと
- `featured:true` - カルーセル用特集コンテンツ

### インタラクティブ機能
- **フルスクリーンナビゲーション**: サイトセクション付き展開可能オーバーレイメニュー
- **読書設定パネル**: フォント選択、文字サイズ制御、トップへ戻る
- **カルーセルシステム**: タッチ/ドラッグサポート付き特集コンテンツ
- **テーマ切り替え**: localStorageの永続化によるダークモード切り替え
- **アートギャラリーフィルタリング**: 外部ライブラリなしのカテゴリベースフィルタリング
- **ランダム記事**: Ghost APIによる動的コンテンツランダム化
- **更新履歴**: 展開可能な最新記事セクション
- **小説進捗追跡**: Ghost APIによる動的話数カウント

### API統合
テーマはGhostのContent APIを広範囲に活用：
- 小説の話数カウントと最終更新日
- ランダム記事生成
- コンテンツフィルタリングと組織化
- メンバー固有のコンテンツ表示

### アセット構造
```
assets/
├── css/
│   ├── screen.css - レスポンシブデザイン対応メインスタイルシート
│   └── post.css - 記事専用スタイル
├── js/
│   ├── main.js - コアテーマ機能
│   └── snowfall.jquery.min.js - 雪アニメーション効果
└── images/ - テーマ画像とアセット
```

## 開発ガイドライン

### Ghostバージョン互換性
- Ghost >= 5.0.0 が必要（package.jsonで指定）
- `{{#get}}` ヘルパーやContent APIなどの最新Ghost機能を使用

### Content API使用方法
テーマは以下の機能のために `window.GHOST_API_KEY` としてGhostのContent APIキーが必要：
- 動的小説メタデータの取得
- ランダム記事生成
- 話数カウント機能

### レスポンシブデザイン
テーマはモバイルファーストで構築され、以下のブレークポイントを持ちます：
- モバイル: ベーススタイル
- タブレット: 768px+
- デスクトップ: 1024px+

### パフォーマンス考慮事項
- 画像の遅延読み込み
- 雪エフェクト用CDNからのjQuery読み込み
- スムーズなインタラクションのためのCSS transition
- JavaScript での効率的なDOM クエリ

### 日本語コンテンツサポート
- 日本語タイポグラフィ用フォントインポート
- 日本語スタイルの日付フォーマット
- 適切な文字エンコーディング（UTF-8）
- 必要に応じた縦書きテキストレイアウトのサポート

## 一般的な開発タスク

### テーマテスト
これはGhostテーマのため、テストにはGhostのインストールが必要です。テーマはGhost の content/themes ディレクトリに配置し、Ghost管理画面から有効化する必要があります。

### アセット管理
- CSSとJSファイルはGhostのassetヘルパー経由で配信： `{{asset "path/to/file"}}`
- 画像も同じassetヘルパーシステムを使用
- ビルドプロセスは不要 - ファイルは直接配信される

### コンテンツ構造の変更
コンテンツクエリやタグ構造を変更する際：
1. 関連するテンプレートファイル（.hbs）を更新
2. main.js のAPI呼び出しが新しいタグ構造と一致することを確認
3. 実際のGhostコンテンツでテストして機能を検証

### JavaScript依存関係
- jQuery 3.4.1（CDNから読み込み）
- 冬エフェクト用Snowfallプラグイン
- その他の機能はすべてvanilla JavaScript

## 重要な注意点

### メンバー機能
テーマにはGhostの組み込みメンバーシップ機能が含まれています：
- メンバー固有のコンテンツ表示
- 非メンバー向けサブスクリプションCTA
- サインアップ/ログイン用メンバーポータル統合

### SEOとメタ
- Ghostの組み込みメタヘルパーを使用
- Ghost経由のSchema.org構造化データ
- `{{ghost_head}}` によるOpen Graphタグ

### ブラウザ互換性
- 最新ブラウザ機能を使用（CSS Grid、Flexbox、Fetch API）
- 未サポート機能に対する適切なフォールバック
- モバイルカルーセルインタラクション用タッチイベント